# QR Code Generator Environment Configuration
# Copy this file to .env.local and fill in your actual values

# Google Sheets Configuration
# The name of your Google Sheet (must match exactly)
SPREADSHEET_NAME=Event Participants Database

# The name of the worksheet within your spreadsheet
SHEET_NAME=Sheet1

# Email Configuration
# Your Gmail address that will send the emails
SENDER_EMAIL=your-email@gmail.com

# Gmail App Password (NOT your regular password)
# Generate this from Google Account Settings > Security > 2-Step Verification > App passwords
SENDER_PASSWORD=your-gmail-app-password

# File Paths Configuration
# Directory where QR codes will be saved
QR_CODES_DIR=qr_codes

# Path to PDF file that will be attached to emails
PDF_ATTACHMENT_PATH=event-schedule.pdf

# Path to HTML email template file
EMAIL_TEMPLATE_PATH=email_template.html

# Optional: Email Subject Line
EMAIL_SUBJECT=Event Confirmation - QR Code Attached

# Optional: SMTP Configuration (Gmail defaults)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587

# Instructions:
# 1. Copy this file to .env.local
# 2. Replace all placeholder values with your actual configuration
# 3. Never commit .env.local to version control (it's in .gitignore)
# 4. Ensure your Gmail account has 2FA enabled to generate App Password
